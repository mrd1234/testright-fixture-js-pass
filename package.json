{
  "name": "testright-fixture-js-pass",
  "version": "1.0.0",
  "private": true,
  "description": "testright verb-battery fixture (green): a tested calculator (run / convert) and an untested email validator (generate).",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
